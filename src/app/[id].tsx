import { StyleSheet, Text, View } from "react-native";
import React from "react";

const GetByIdScreen = () => {
  return (
    <View>
      <Text>GetByIdScreen</Text>
    </View>
  );
};

export default GetByIdScreen;

const styles = StyleSheet.create({});
